{
  "name": "nextjs-mfe-boilerplate",
  "private": true,
  "description": "Next.js 14 with Module Federation",
  "version": "0.0.0",
  "packageManager": "pnpm@8.14.1",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "start": "turbo run start --parallel",
    "build": "turbo run build --parallel"
  },
  "devDependencies": {
    "turbo": "^2.0.3"
  }
}
